# Copy this file to .env for local dev with `vercel dev`, or set the
# same variable name in your hosting provider's environment variable
# settings (e.g. Vercel: Project Settings > Environment Variables).
# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
